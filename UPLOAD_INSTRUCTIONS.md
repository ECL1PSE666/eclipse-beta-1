# 📁 Инструкция по загрузке файлов на GitHub (решение проблемы "Too many files")

## 🚨 Проблема:
GitHub показывает: "Yowza, that's a lot of files. Try uploading fewer than 100 at a time."

## ✅ Решение: Загружать файлы частями

### Способ 1: Загружать по папкам (рекомендуется)

**Шаг 1: Загрузите основные файлы конфигурации (5-10 файлов)**
1. На GitHub нажмите "uploading an existing file"
2. Перетащите ЭТИ файлы:
   ```
   package.json
   next.config.ts
   tsconfig.json
   tailwind.config.js
   postcss.config.js
   eslint.config.js
   .gitignore
   ```

**Шаг 2: Загрузите папку src/ (самая большая)**
1. Создайте ZIP архив папки src/ на вашем компьютере
2. ИЛИ загружайте файлы из src/ по 20-30 штук за раз

**Шаг 3: Загрузите папку public/ (5-6 файлов)**
1. Перетащите все файлы из папки public/

### Способ 2: Создать ZIP архив

1. **Создайте архив с нужными файлами:**
   - Откройте папку `eclipse`
   - Выделите все файлы КРОМЕ:
     - `node_modules` (папка)
     - `.next` (папка) 
     - `out` (папка)
     - `*.log` (файлы)
     - `.env.local` (файл)
   - Нажмите правой кнопкой → "Отправить в" → "Сжатая папка"

2. **Загрузите архив на GitHub:**
   - Перетащите ZIP файл в GitHub
   - GitHub автоматически распакует файлы

### Способ 3: Использовать git (если установите)

1. Установите Git с [git-scm.com](https://git-scm.com/download/win)
2. В командной строке в папке eclipse:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/eclipse-video-platform.git
   git push -u origin main
   ```

## 🎯 Что обязательно нужно загрузить:

**КОНФИГУРАЦИОННЫЕ ФАЙЛЫ (самые важные):**
- `package.json` - зависимости проекта
- `next.config.ts` - конфигурация Next.js
- `tsconfig.json` - настройки TypeScript
- `tailwind.config.js` - стили Tailwind
- `.gitignore` - игнорируемые файлы

**ИСХОДНЫЙ КОД:**
- Папка `src/` - весь код приложения
- Папка `public/` - изображения и статические файлы

## ❌ Что НЕ нужно загружать:
- `node_modules/` - слишком большой
- `.next/` - генерируется автоматически
- `out/` - результат сборки
- `*.log` - логи
- `.env.local` - секретные данные

## ⏱️ Время загрузки:
- По частям: 15-20 минут
- Через архив: 5 минут

## 💡 Совет:
Если ничего не помогает, начните с создания пустого репозитория на GitHub и загружайте файлы по одному, начиная с самых важных (package.json, src/ папка).
